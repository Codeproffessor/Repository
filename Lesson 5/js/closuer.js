/*function counter() {
  let count = 0;
  function incrementCounter() {
    count++;
    return count;
  }
  return incrementCounter;
}

const count = 15;

const incrementCounter = counter();
incrementCounter()
incrementCounter();
*/
